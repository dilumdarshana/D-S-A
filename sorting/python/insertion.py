def insertion_sort(nums: list[int]) -> int:

  for num in range(1, len(nums)):
    key = nums[num]
    i = num - 1
    
    while i > 0:
      if nums[i] < key:
        



insertion_sort([12, 11, 13, 5, 6])
